package com.wecom.scrm.config;

public class RedisKeys {
    public static final String WECOM_ACCESS_TOKEN = "wecom:access_token:corp";
    
    // We can add more keys here (e.g., ticket for jsapi)
}
